seed: 0
device: auto

checkpoint:
  simclr_path: /home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/checkpoints/cifar10_simclr_smoke.pt

model:
  feature_dim: 512
  num_classes: 10

dataset:
  name: cifar10
  data_root: /home/yeyee/research/03_datasets/SimCLR-Reproduction-and-Ablation
  train: true
  download: false
  augmentation_strength: weak

training:
  epochs: 1
  batch_size: 32
  max_train_batches: 10
  num_workers: 2
  shuffle: true

optimizer:
  name: adam
  lr: 0.001
  weight_decay: 0.0

paths:
  log_dir: results/logs
  log_filename: cifar10_linear_probe_smoke.csv
  # Disabled by default so scaffold smoke runs do not write model weights.
  # If enabled, checkpoint_dir must be outside the Git repository.
  save_checkpoint: false
  checkpoint_dir: /home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/linear_probe
  checkpoint_filename: cifar10_linear_probe_smoke.pt
