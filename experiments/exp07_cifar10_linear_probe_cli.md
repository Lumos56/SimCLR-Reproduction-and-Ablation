# Experiment 07: CIFAR-10 Linear Probe Smoke CLI Run

## Purpose

Record the Human Owner's manual real CIFAR-10 linear probe smoke CLI run.

This is a real CIFAR-10 smoke run only. It is not a full linear probe experiment, not a supervised baseline, and not a real model performance result.

## Command

Run by the Human Owner:

```bash
/home/yeyee/miniconda3/envs/simclr/bin/python -m src.train_linear_probe --config configs/cifar10_linear_probe_smoke.yaml
```

## Observed Output

```text
completed linear probe smoke training: steps=10 final_train_loss=3.629154 final_train_acc=0.062500
log_path=results/logs/cifar10_linear_probe_smoke.csv
```

## CSV Log

Repository log path:

```text
results/logs/cifar10_linear_probe_smoke.csv
```

File provenance:

- This CSV was generated by the Human Owner's manual CLI run.
- Codex did not rerun training to create this CSV.
- Codex did not edit the CSV content; Codex only read it and recorded it as a small smoke log.

Content:

```csv
epoch,step,train_loss,train_acc
1,1,3.8531181812286377,0.125
1,2,4.169179916381836,0.0625
1,3,3.4267210960388184,0.15625
1,4,3.5006487369537354,0.0625
1,5,3.2068593502044678,0.09375
1,6,2.528350591659546,0.09375
1,7,2.782953977584839,0.125
1,8,3.1626577377319336,0.125
1,9,2.6127138137817383,0.1875
1,10,3.6291537284851074,0.0625
```

## Metrics Recorded

| Field | Value |
|---|---|
| Dataset | CIFAR-10 |
| Run type | Linear probe smoke |
| Steps | 10 |
| Final train loss | `3.629154` |
| Final train accuracy | `0.062500` |

## Interpretation

- This run verifies that the CIFAR-10 linear probe smoke CLI can execute with the external SimCLR checkpoint and external CIFAR-10 data.
- `final_train_acc=0.062500` is not a real evaluation result.
- The train accuracy from this 1 epoch / 10 batch smoke run must not be reported as model performance.
- This record does not establish convergence, baseline quality, or final SimCLR representation quality.

## Checkpoint Behavior

The config has:

```yaml
save_checkpoint: false
```

Therefore no linear probe checkpoint was expected.

Observed by the Human Owner:

- No linear probe checkpoint was saved.
- `/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/linear_probe` does not exist.
- This is expected for this smoke run.

## Repository Safety Check

Observed by the Human Owner:

- `results/logs/cifar10_linear_probe_smoke.csv` is a small CSV log and may be committed.
- No repository data files were found.
- No `.pt`, `.pth`, `.ckpt`, or `.onnx` files were found inside the repository.
- No files over 10MB were found inside the repository.

## Limitations

- Only 1 epoch and 10 batches were run.
- This is not a full linear probe experiment.
- This is not a baseline result.
- This is not a real evaluation result.
- No supervised baseline, evaluation report, ablation, or long run was performed.

## Next Step

Review and commit this Task 20 record, then decide whether the next step should be supervised baseline scaffolding or evaluation planning.
