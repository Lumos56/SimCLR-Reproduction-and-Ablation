# Experiment 13: CIFAR-10 Linear Probe Short

## Purpose

Record the Human Owner's linear probe short training run on CIFAR-10 using the short SimCLR checkpoint.

This is a linear probe short training run using the short-baseline config. It is not a smoke run, not a CIFAR-10 test-set evaluation result, and not a final model performance result.

## Command

Run by the Human Owner:

```bash
/home/yeyee/miniconda3/envs/simclr/bin/python -m src.train_linear_probe --config configs/cifar10_linear_probe_short.yaml
```

## Config

```text
configs/cifar10_linear_probe_short.yaml
```

Key config conditions:

- Dataset: CIFAR-10
- Data root: `/home/yeyee/research/03_datasets/SimCLR-Reproduction-and-Ablation`
- Download: `false`
- SimCLR checkpoint: `/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/simclr_short/cifar10_simclr_short.pt`
- Device: `auto`
- Epochs: `5`
- Batch size: `128`
- Max train batches: `null`, meaning full epoch
- Linear probe checkpoint directory: `/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/linear_probe_short`

## Observed Output

```text
completed linear probe smoke training: steps=1950 final_train_loss=1.039513 final_train_acc=0.664062
log_path=results/logs/cifar10_linear_probe_short.csv
checkpoint_path=/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/linear_probe_short/cifar10_linear_probe_short.pt
```

The script printed `completed linear probe smoke training`, but the command used `configs/cifar10_linear_probe_short.yaml`. This record treats the run as linear probe short training, not as a smoke run.

## CSV Log

Repository log path:

```text
results/logs/cifar10_linear_probe_short.csv
```

File summary:

- Size: about 68K
- Lines: 1951 including header
- The CSV is a small training log and may be committed.
- The CSV was generated by the Human Owner's training run.
- Codex did not rerun training to create this CSV.
- Codex did not edit the CSV content; Codex inspected it with `wc`, `head`, and `tail`.

First rows:

```csv
epoch,step,train_loss,train_acc
1,1,2.309105157852173,0.0625
1,2,2.306161642074585,0.0703125
1,3,2.2364182472229004,0.1484375
1,4,2.241469383239746,0.171875
```

Last rows:

```csv
5,1941,1.235630750656128,0.53125
5,1942,1.1438193321228027,0.6171875
5,1943,1.1534194946289062,0.59375
5,1944,1.095288872718811,0.6015625
5,1945,1.2004001140594482,0.6015625
5,1946,1.041774034500122,0.609375
5,1947,1.192845106124878,0.578125
5,1948,1.1267486810684204,0.640625
5,1949,0.9921101331710815,0.6484375
5,1950,1.0395134687423706,0.6640625
```

## Metrics Recorded

| Field | Value |
|---|---|
| Dataset | CIFAR-10 |
| Run type | Linear probe short training |
| Steps | 1950 |
| Final train loss | `1.039513` |
| Final train accuracy | `0.664062` |

## Interpretation

- This is the linear probe short training run on the short SimCLR checkpoint.
- `final_train_acc=0.664062` is the last logged training-batch accuracy.
- The final train accuracy is not test accuracy.
- This run does not include CIFAR-10 test-set evaluation.
- The final train accuracy must not be reported as final model performance.
- A separate approved evaluation or summary task is needed before making any baseline-performance claim.

## External Checkpoint

Checkpoint path:

```text
/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/linear_probe_short/cifar10_linear_probe_short.pt
```

Observed checkpoint size:

```text
about 65K
```

This checkpoint is outside the Git repository, which is correct. Model weights must not be committed.

## Repository Safety Check

- `results/logs/cifar10_linear_probe_short.csv` is untracked and may be committed as a small CSV log.
- No repository data files were found.
- No `.pt`, `.pth`, `.ckpt`, or `.onnx` files were found inside the repository.
- No files over 10MB were found inside the repository.

## Not Run

- Codex did not rerun linear probe short training.
- No evaluation was run.
- No supervised baseline was run.
- No ablation was run.
- No new checkpoint was created by Codex.
- The external checkpoint was not moved or deleted.
- The training accuracy was not reported as test accuracy or final performance.

## Next Step

Review and commit this Task 30 record, then decide the short baseline summary and evaluation planning task.
