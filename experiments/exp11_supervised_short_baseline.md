# Experiment 11: CIFAR-10 Supervised Short Baseline

## Purpose

Record the Human Owner's supervised short baseline training run on CIFAR-10.

This is a supervised short baseline training run. It is not a final performance result and does not include evaluation on the CIFAR-10 test set.

## Command

Run by the Human Owner:

```bash
/home/yeyee/miniconda3/envs/simclr/bin/python -m src.train_supervised --config configs/cifar10_supervised_short.yaml
```

## Config

```text
configs/cifar10_supervised_short.yaml
```

Key config conditions:

- Dataset: CIFAR-10
- Data root: `/home/yeyee/research/03_datasets/SimCLR-Reproduction-and-Ablation`
- Download: `false`
- Device: `auto`
- Epochs: `10`
- Batch size: `128`
- Max train batches: `null`, meaning full epoch
- Checkpoint saving: `true`
- Checkpoint directory: `/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/supervised_short`

## Observed Output

```text
completed supervised smoke training: steps=3900 final_train_loss=0.191644 final_train_acc=0.937500
log_path=results/logs/cifar10_supervised_short.csv
```

## CSV Log

Repository log path:

```text
results/logs/cifar10_supervised_short.csv
```

File summary:

- Size: about 138K
- Lines: 3901 including header
- The CSV is a small training log and may be committed.
- The CSV was generated by the Human Owner's training run.
- Codex did not rerun training to create this CSV.
- Codex did not edit the CSV content; Codex inspected it with `wc`, `head`, and `tail`.

First rows:

```csv
epoch,step,train_loss,train_acc
1,1,2.330171585083008,0.125
1,2,2.7969186305999756,0.1328125
1,3,2.313310146331787,0.1875
1,4,2.3221187591552734,0.2109375
```

Last rows:

```csv
10,3891,0.2847804129123688,0.8828125
10,3892,0.21978773176670074,0.9296875
10,3893,0.30407509207725525,0.90625
10,3894,0.22592192888259888,0.921875
10,3895,0.27826201915740967,0.8984375
10,3896,0.25010114908218384,0.9140625
10,3897,0.2551574110984802,0.890625
10,3898,0.35601943731307983,0.875
10,3899,0.18954691290855408,0.9140625
10,3900,0.19164372980594635,0.9375
```

## Metrics Recorded

| Field | Value |
|---|---|
| Dataset | CIFAR-10 |
| Run type | Supervised short baseline training |
| Steps | 3900 |
| Final train loss | `0.191644` |
| Final train accuracy | `0.937500` |

## Interpretation

- This is the supervised short baseline training run.
- `final_train_acc=0.937500` is the last logged training-batch accuracy, not test accuracy.
- This run does not include evaluation on the CIFAR-10 test set.
- The last logged training-batch accuracy must not be reported as final model performance.
- This run is useful as a supervised short-baseline training record, but final performance requires a separate approved evaluation step.

## External Checkpoint

Checkpoint path:

```text
/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/supervised_short/cifar10_supervised_short.pt
```

Observed checkpoint size:

```text
about 129M
```

This checkpoint is outside the Git repository, which is correct. Model weights must not be committed.

## Repository Safety Check

- `results/logs/cifar10_supervised_short.csv` is untracked and may be committed as a small CSV log.
- No repository data files were found.
- No `.pt`, `.pth`, `.ckpt`, or `.onnx` files were found inside the repository.
- No files over 10MB were found inside the repository.

## Log Display Issue

The initial command template used `cat results/logs/cifar10_supervised_short.csv`, which printed thousands of lines to the terminal.

This was not a code failure. It was a workflow/log-display mistake.

For short-baseline, baseline, or long-run CSV logs, use `wc -l`, `head -n 5`, and `tail -n 10` instead of printing the whole CSV.

## Not Run

- Codex did not rerun supervised short baseline training.
- No evaluation was run.
- No ablation was run.
- No new checkpoint was created by Codex.
- The external checkpoint was not moved or deleted.
- No test accuracy or final model performance was reported.

## Next Step

Review and commit this Task 28 record, then approve the SimCLR short pretrain as a separate task.
