# Experiment 32: Batch64 Linear Probe Short

## Purpose

Record the Human Owner-run batch64 linear probe short training result using:

```text
configs/cifar10_linear_probe_batch64_short.yaml
```

This task records the linear probe training stage only. It does not run evaluation, ablation-table generation, figure generation, tests, or any new training.

The training script printed `completed linear probe smoke training`, but the command used the batch64 linear probe short ablation config. Therefore this record treats the run as batch64 linear probe short training, not as a smoke run.

## Config

- Dataset: CIFAR-10.
- Dataset root: `/home/yeyee/research/03_datasets/SimCLR-Reproduction-and-Ablation`.
- `dataset.train`: `true`.
- `dataset.download`: `false`.
- `dataset.augmentation_strength`: `weak`.
- SimCLR checkpoint path: `/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/simclr_batch64_short/cifar10_simclr_batch64_short.pt`.
- Encoder source: batch64 SimCLR short pretrain checkpoint.
- Epochs: 5.
- Linear probe batch size: 128.
- Max train batches: `null`, meaning full epoch.
- `paths.save_checkpoint`: `true`.
- Device: `auto`.
- Config path: `configs/cifar10_linear_probe_batch64_short.yaml`.
- Seed: 0.

## Command

Run by the Human Owner:

```bash
/home/yeyee/miniconda3/envs/simclr/bin/python -m src.train_linear_probe --config configs/cifar10_linear_probe_batch64_short.yaml
```

## Observed Output

```text
completed linear probe smoke training: steps=1950 final_train_loss=1.074155 final_train_acc=0.625000
log_path=results/logs/cifar10_linear_probe_batch64_short.csv
checkpoint_path=/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/linear_probe_batch64_short/cifar10_linear_probe_batch64_short.pt
```

## CSV Log

- Path: `results/logs/cifar10_linear_probe_batch64_short.csv`.
- Size observed by Codex: `68K`.
- Lines observed by Codex: 1951 including header.
- Header: `epoch,step,train_loss,train_acc`.
- The CSV is a small training log and may be committed.
- The CSV was generated by the Human Owner's training run.
- Codex did not rerun training to create this CSV.
- Codex did not edit the CSV content; Codex inspected it with `wc`, `ls`, `head`, and `tail`.

First logged row:

```csv
epoch,step,train_loss,train_acc
1,1,2.2733020782470703,0.1484375
```

Final logged row:

```csv
5,1950,1.0741548538208008,0.625
```

## Metrics Recorded

| Field | Value |
|---|---:|
| Steps | 1950 |
| Final logged train loss | 1.0741548538208008 |
| Final logged train accuracy | 0.625 |

The observed command-line summary rounded these values as:

```text
final_train_loss=1.074155 final_train_acc=0.625000
```

## External Checkpoint

Checkpoint path:

```text
/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/linear_probe_batch64_short/cifar10_linear_probe_batch64_short.pt
```

Observed checkpoint size:

```text
65K
```

This checkpoint is outside the Git repository, which is correct. Model weights must not be committed.

Checkpoint quick check observed by Codex:

- Exists: `True`.
- Top-level keys: `['epoch', 'step', 'classifier_state_dict', 'optimizer_state_dict']`.
- PASS: checkpoint loaded successfully.

## Interpretation

- This is the linear probe training stage for the batch-size ablation.
- The encoder comes from the batch64 SimCLR short pretrain checkpoint.
- `final_train_loss=1.0741548538208008` is training loss.
- `final_train_acc=0.625` is last-batch training accuracy.
- The final train accuracy is not test accuracy.
- The final train accuracy is not final SimCLR performance or final project performance.
- The real batch-size ablation metric will come from Task 54 after CIFAR-10 test-set evaluation.
- Task 55 will compare batch64 against the batch128 baseline.

The batch-size comparison remains preliminary because this setup uses fixed epochs, not fixed optimizer steps. Batch64 SimCLR pretraining used more optimizer steps per epoch than the batch128 short baseline.

## Repository Safety Check

- `results/logs/cifar10_linear_probe_batch64_short.csv` is a small CSV log and should be included in the Task 53 commit.
- No repository data files were reported.
- No `.pt`, `.pth`, `.ckpt`, or `.onnx` files were found inside the repository by Codex.
- No files over 10MB were found inside the repository by Codex.
- The checkpoint remains in external model storage.

## Not Done

- Codex did not rerun linear probe training.
- Codex did not run evaluation.
- Codex did not run tests.
- Codex did not create checkpoints.
- Codex did not create figures.
- Codex did not edit result tables.
- Codex did not start Task 54.
- No test accuracy claim was made.
- No final SimCLR performance claim was made.

## Next Step

Task 54 should run and record the batch64 linear probe CIFAR-10 test-set evaluation result after Human Owner approval.
