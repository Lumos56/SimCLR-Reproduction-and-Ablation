# Experiment 22: No Projection Linear Probe Short

## Purpose

Record the Human Owner's no-projection linear probe short training run on CIFAR-10 using the no-projection SimCLR short checkpoint.

This is a no-projection linear probe short training run using the approved ablation config. It is not a smoke run, not a CIFAR-10 test-set evaluation result, and not the no-projection ablation test metric.

## Command

Run by the Human Owner:

```bash
/home/yeyee/miniconda3/envs/simclr/bin/python -m src.train_linear_probe --config configs/cifar10_linear_probe_no_projection_short.yaml
```

## Config

```text
configs/cifar10_linear_probe_no_projection_short.yaml
```

Key config conditions:

- Dataset: CIFAR-10
- Data root: `/home/yeyee/research/03_datasets/SimCLR-Reproduction-and-Ablation`
- Download: `false`
- SimCLR checkpoint: `/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/simclr_no_projection_short/cifar10_simclr_no_projection_short.pt`
- Device: `auto`
- Epochs: `5`
- Batch size: `128`
- Max train batches: `null`, meaning full epoch
- Save checkpoint: `true`
- Linear probe checkpoint directory: `/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/linear_probe_no_projection_short`

## Observed Output

```text
completed linear probe smoke training: steps=1950 final_train_loss=1.125928 final_train_acc=0.601562
log_path=results/logs/cifar10_linear_probe_no_projection_short.csv
checkpoint_path=/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/linear_probe_no_projection_short/cifar10_linear_probe_no_projection_short.pt
```

The script printed `completed linear probe smoke training`, but the command used `configs/cifar10_linear_probe_no_projection_short.yaml`. This record treats the run as no-projection linear probe short training, not as a smoke run.

## CSV Log

Repository log path:

```text
results/logs/cifar10_linear_probe_no_projection_short.csv
```

File summary:

- Size: about 68K
- Lines: 1951 including header
- The CSV is a small training log and may be committed.
- The CSV was generated by the Human Owner's training run.
- Codex did not rerun training to create this CSV.
- Codex did not edit the CSV content; Codex inspected it with `wc`, `ls`, `head`, and `tail`.

First rows:

```csv
epoch,step,train_loss,train_acc
1,1,2.3018620014190674,0.0703125
1,2,2.2799386978149414,0.15625
1,3,2.2802963256835938,0.15625
1,4,2.2720980644226074,0.1796875
```

Last rows:

```csv
5,1941,1.31095290184021,0.578125
5,1942,1.1871004104614258,0.609375
5,1943,1.2664401531219482,0.5625
5,1944,1.1773288249969482,0.5546875
5,1945,1.2783048152923584,0.5
5,1946,1.177557110786438,0.53125
5,1947,1.2898873090744019,0.546875
5,1948,1.2836495637893677,0.5078125
5,1949,1.0903950929641724,0.65625
5,1950,1.1259280443191528,0.6015625
```

## Metrics Recorded

| Field | Value |
|---|---|
| Dataset | CIFAR-10 |
| Run type | No-projection linear probe short training |
| Steps | 1950 |
| Final train loss | `1.125928` |
| Final train accuracy | `0.601562` |

## Interpretation

- This is the no-projection linear probe short training run on the no-projection SimCLR short checkpoint.
- `final_train_acc=0.601562` is the last logged training-batch accuracy.
- The final train accuracy is not test accuracy.
- This run does not include CIFAR-10 test-set evaluation.
- This is not the no-projection ablation test-set metric.
- The no-projection ablation test-set metric must be produced later by Task 43.
- The final train accuracy must not be reported as final model performance.

## External Checkpoint

Checkpoint path:

```text
/home/yeyee/research/04_models/SimCLR-Reproduction-and-Ablation/linear_probe_no_projection_short/cifar10_linear_probe_no_projection_short.pt
```

Observed checkpoint size:

```text
about 65K
```

This checkpoint is outside the Git repository, which is correct. Model weights must not be committed.

## Repository Safety Check

- `results/logs/cifar10_linear_probe_no_projection_short.csv` is untracked and may be committed as a small CSV log.
- No repository data files were reported by the Human Owner.
- No `.pt`, `.pth`, `.ckpt`, or `.onnx` files were reported inside the repository.
- No files over 10MB were reported inside the repository.
- Codex safety checks found no `.pt`, `.pth`, `.ckpt`, or `.onnx` files inside the repository.
- Codex safety checks found no files over 10MB inside the repository.
- No disallowed `src/*`, `configs/*`, `tests/*`, `README.md`, `AGENTS.md`, or `notes/decision_log.md` paths were listed as modified.

## Not Run

- Codex did not rerun no-projection linear probe short training.
- Codex did not run evaluation.
- Codex did not run supervised baseline.
- Codex did not run ablation analysis.
- Codex did not create a new checkpoint.
- Codex only inspected the external checkpoint with read-only `ls -lh`; Codex did not load, move, delete, or edit it.
- The external checkpoint was not moved or deleted.
- The training accuracy was not reported as test accuracy or final performance.

## Next Step

Review and commit this Task 42 record, then approve Task 43 no-projection linear probe evaluation.
