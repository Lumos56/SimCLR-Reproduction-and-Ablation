name: simclr
# Gate 0 verified by the Human Owner on 2026-05-26:
# Python 3.10.20
# torch 2.11.0+cu128
# torchvision 0.26.0+cu128
# torch.version.cuda 12.8
# GPU: NVIDIA GeForce RTX 5080
# CUDA available: True
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - matplotlib
  - scikit-learn
  - pyyaml
  - tqdm
  - pytest
  - tensorboard
  - pip:
      # PyTorch / torchvision should be installed using the official PyTorch selector.
      # Do not guess CUDA wheel versions here.
      # Add exact torch/torchvision entries only after Gate 0 is verified.
